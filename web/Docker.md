## Build Docker Image
```shell
# 1. Build Docker image (latest only)
docker build -t korvisedyotsaiy/bkia-website-client:latest .

# 2. Push image to ECR
docker push korvisedyotsaiy/bkia-website-client:latest
```
