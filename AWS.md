```shell
# Remote to EC2 ubuntu server
ssh -i bkia-website.pem ubuntu@43.210.51.192

# HTTPS with Credential Caching (Not recommended for EC2)
git config --global credential.helper store

# Check disk space usage
df -h
```
