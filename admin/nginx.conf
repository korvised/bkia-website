server {
  listen 80;

  root   /usr/share/nginx/html;
  index  index.html index.htm;

  # Serve the admin SPA under /admin
  location /admin/ {
    try_files $uri $uri /admin/index.html;
  }

  error_page 500 502 503 504 /50x.html;
  location = /50x.html {
    root html;
  }
}
